/*
array
1n < 0
2n > 0
3* == 0
n , [0]
*/
#include <bits/stdc++.h>
using namespace std;
void fill(vector s, int n);
auto main()->int{
    int n;
    cin >> n;
    vector<int>s;
    for(int i=0; i<n; i++){
        cin>>x;
        s.push_back(x);
    }
    if(s[0]<0){

    }
    else()
}

void fill(vector x, int n){
    int a=1, j;
    while(a%2!=0){
        
        a++;
    }
}